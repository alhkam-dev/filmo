spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/films
    username: db
    password: db
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: validate
  application:
    name: Filmo